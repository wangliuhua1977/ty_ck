<UserControl x:Class="Ty.Ck.Desktop.Views.Pages.NotificationRecordsPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.1*" />
            <ColumnDefinition Width="0.9*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Margin="0,0,16,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="16" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="通知记录列表" Style="{StaticResource SectionTitleTextStyle}" />
                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Border Padding="16" Background="{StaticResource AccentSubtleBrush}" BorderBrush="{StaticResource AccentStrokeBrush}" BorderThickness="1" CornerRadius="16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="南区 1 号门摄像机" FontWeight="SemiBold" />
                                    <TextBlock Margin="0,6,0,0" Text="巡检触发" Style="{StaticResource SecondaryTextStyle}" />
                                </StackPanel>
                                <TextBlock Grid.Column="1" Text="已发送" Foreground="{StaticResource SuccessBrush}" />
                            </Grid>
                        </Border>
                        <Border Margin="0,10,0,0" Padding="16" Background="{StaticResource SurfaceAltBrush}" CornerRadius="16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="厂区东侧球机" FontWeight="SemiBold" />
                                    <TextBlock Margin="0,6,0,0" Text="人工触发" Style="{StaticResource SecondaryTextStyle}" />
                                </StackPanel>
                                <TextBlock Grid.Column="1" Text="待实现" Style="{StaticResource SecondaryTextStyle}" />
                            </Grid>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>

        <StackPanel Grid.Column="1">
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="海报预览" Style="{StaticResource SectionTitleTextStyle}" />
                    <Border Margin="0,18,0,0"
                            Height="360"
                            Background="{StaticResource SurfaceAltBrush}"
                            BorderBrush="{StaticResource StrokeBrush}"
                            BorderThickness="1"
                            CornerRadius="20">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock HorizontalAlignment="Center" Text="单图海报占位" FontSize="24" FontWeight="SemiBold" />
                            <TextBlock Margin="0,8,0,0" Text="企业微信发送留待后续轮次接入" Style="{StaticResource SecondaryTextStyle}" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorderStyle}" Margin="0,16,0,0">
                <StackPanel>
                    <TextBlock Text="发送信息" Style="{StaticResource SectionTitleTextStyle}" />
                    <TextBlock Margin="0,18,0,0" Text="@ 维护人手机号" />
                    <TextBlock Margin="0,8,0,0" Text="中文 UTF-8 海报" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
